# Diet Tracking Backend (Node + Express, file DB)

Prosty backend dla frontend-u diet-tracking. Używa `db.json` jako magazynu.

## Instalacja

1. Skopiuj pliki do katalogu, np. `diet-backend`.
2. Zainstaluj zależności:
   ```bash
   npm install
